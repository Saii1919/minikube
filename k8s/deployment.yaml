apiVersion: apps/v1
kind: Deployment
metadata:
  name: digital-marketing
spec:
  replicas: 2
  selector:
    matchLabels:
      app: digital-marketing
  template:
    metadata:
      labels:
        app: digital-marketing
    spec:
      containers:
        - name: digital-marketing
          image: Saii1919/saikumar:v1
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "128Mi"
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 20
